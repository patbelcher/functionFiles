//pie factory

//create a function that makes pies

//repeat the string as much as the quantity of the order


function makePie(quantity="stranger") {
    //const pieOrder = `🥧`.repeat(quantity);
    const pieOrder = "hello there, " + quantity + " ";
    return pieOrder;
}
const result = makePie();
console.log(result);

